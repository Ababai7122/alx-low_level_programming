#include"main.h"
#include <stdio.h>

/**
 * add -  adds two integers and returns the result.
 *
 * @x: first number
 * @y: second number
 *
 * Return: sum of a & b
 *
 */

int add(int x, int y)
{
	int sum = a + b;

	return (sum);
}
